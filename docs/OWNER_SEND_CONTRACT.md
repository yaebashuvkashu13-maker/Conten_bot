# Owner send contract — DO NOT REGRESS

This is the standing agreement with the VPS owner. Agents must not “soft-ship”
around these rules for SLA excuses.

## Combat shooters: PUBG / Standoff / WoT

- Each Telegram send = **склейка ×3** (three gate-passed fights), not ×1/×2.
- Daily quotas were **lowered** specifically because ×3 is harder.
- `SHIP_PARTIAL` / `SOFT_MIN_CLIPS=1` / `EARLY_SHIP` must **not** drop below 3
  for these games (`_montage_soft_min_clips`).

## MLBB

- Popular scene = **teamfight** with streak banner **triple+** (not lane doubles).
- Fast-ship default `MLBB_BANNER_FAST_SHIP_MIN_TIER=3`.
- No mid-peak slice junk; no early laning (`min_peak` floor).

## Genshin

- Clip from **fight start → fight end** (HP bar), not a mid-peak slice.

## Ops

- Prefer no send over trash send.
- Disk full / hang: self-heal, then resume quality path — never filler ×1.
